# Write a complete program that reads in an integer with a suitable output message, 
# store it in a variable, add 1 to the variable, and then print the variable out to the screen.

var_input = int(input("Enter a integer: "))
print(var_input+1)